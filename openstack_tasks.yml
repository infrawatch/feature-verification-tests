---

- name: Openstack tasks
  hosts: "{{ osp_tasks_host | default('undercloud') }}"
  gather_facts: False
  roles:
    - post_install/openstack_tasks

- name: Collectd tasks
  hosts: undercloud-0
  roles:
    - post_install/collectd_query
  become: True
  become_method: sudo
  become_user: root

