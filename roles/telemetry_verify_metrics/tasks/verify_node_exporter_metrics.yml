- name: Use openstack observabilityclient to verify Node Exporter metrics are stored in prometheus
  ansible.builtin.shell: |
    {{ openstack_cmd }} metric show --disable-rbac node_exporter_build_info
  register: result
  failed_when: not "node_exporter_build_info" in result.stdout or result.rc >= 1
